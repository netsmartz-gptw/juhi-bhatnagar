import React, {useState} from 'react'
import ProductServiceForm from '../note-form/ProductServiceForm'

const ProductServiceAdd = (props) => {
    return(
        <ProductServiceForm/>
    )
}

export default ProductServiceAdd